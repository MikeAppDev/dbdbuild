<div class="container">
    <header class="d-flex flex-wrap justify-content-between align-items-center py-3 mb-4 border-bottom">
        <a class="d-flex align-items-center justify-content-center mb-md-0 me-md-auto text-dark text-decoration-none" href="{{ path('app_home') }}">
            <span class="fs-4 d-flex align-items-center rounded border border-secondary p-2">Home Build</span>
        </a>
        

            <ul class="nav nav-pills">

            {% if app.user %}
                Welcome : 
                <span style="color: green">
                {{ app.user.firstname }} - {{ app.user.name }}
                </span>
                {# {{ dump(app.user) }} #}
                <a class='' href="{{ path('app_logout') }}">Logout</a>
            {# {{dump(app.user)}} #}
            {% else %}
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('app_login') }}">Sign in</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link  text-dark" href="{{ path('app_register' )}}">Create Account</a>
                </li>
            {% endif %}
                
            </ul>
        
    </header>
</div>