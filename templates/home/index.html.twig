{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <main class="container">
        <div class="row">
            <div class="col-md-8">
                <h3 class="pb-4 mb-4 border-bottom"> Last Build </h3>
                <div id="build-list">
                    {% include 'build/list.html.twig' with {builds} %}
                </div>
            </div>
            <div class="col-md-4">
                <div class="position-sticky" style="top: 2rem;">
                    {{ include('widget/widget.html.twig') }}
                    {{ include('widget/category.html.twig') }}
                    {{ include('widget/killer.html.twig') }}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
