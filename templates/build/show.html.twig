{% extends 'base.html.twig' %}

{% block title %}{{ build.title }}{% endblock %}

{% block body %}
<div class="container">
    <h1>{{ build.title }}</h1>

    <p>{{ build.content|raw }}</p>
</div>

{% endblock %}
